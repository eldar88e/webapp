# Omakase Ruby styling for Rails
inherit_gem: { rubocop-rails-omakase: rubocop.yml }

AllCops:
  NewCops: enable
  TargetRubuVersion: 3.3.3
  Exclude:
    - 'db/**/*'

Style/Documentation:
  Enabled: false

# Use `[a, [b, c]]` not `[ a, [ b, c ] ]`
Layout/SpaceInsideArrayLiteralBrackets:
   Enabled: false

Style/StringLiterals:
  EnforcedStyle: single_quotes
