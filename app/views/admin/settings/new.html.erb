<div class="px-4 pt-6">
  <h1 class="text-2xl font-bold">Новая настройка</h1>
  <%= render "form", setting: @setting, method: :post %>
  <br>
  <%= link_to "Back to settings", admin_settings_path, class: "px-4 py-2 bg-blue-500 text-white font-semibold rounded" %>
  <div class="pt-6"></div>
</div>
