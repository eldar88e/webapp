<div id="<%= dom_id cart_item %>" class="cart-item">
  <div class="basis-1/3">
    <%= format_name cart_item.product.name %>
  </div>
  <div class="quantity">
    <%= button_to '-', cart_item_path(quantity: cart_item.quantity-1), method: :patch, class: 'plus' %>
    <%= cart_item.quantity %>
    <%= button_to '+', cart_item_path(quantity: cart_item.quantity+1), method: :patch, class: 'plus' %>
  </div>
   <div class="">
     <%= cart_item.product.price.to_i %>₽
   </div>
</div>
