<!DOCTYPE html>
<html lang="ru">
<html data-controller="dark">
  <head>
    <title>Admin panel</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= vite_client_tag %>
    <%= vite_stylesheet_tag "application" %>
    <%= vite_stylesheet_tag "admin" %>
    <%= vite_javascript_tag "admin" %>
  </head>

  <body class="dark:bg-gray-900 antialiased">
    <header>
      <%= render partial: '/layouts/partials/admin/header' %>
    </header>

    <div class="body">
      <aside id="sidebar" class="sidebar"><%= render partial: '/layouts/partials/admin/aside' %></aside>

      <div class="sidebarBackdrop" id="sidebarBackdrop"></div>

      <div class="main-content bg-white border-b dark:bg-gray-800" id="main-content">
        <main>
          <%= yield %>
        </main>

        <%= render partial: '/layouts/partials/admin/footer' %>
      </div>
    </div>
  </body>
</html>
