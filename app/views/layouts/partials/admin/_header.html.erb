<%= render '/layouts/partials/notices' %>

<nav class="nav-bar">
  <div class="nav-wrapper">
    <div class="flex justify-between items-center">
      <div class="flex items-center justify-start">
        <button class="btn-menu" data-controller="menu-btn" data-action="click->menu-btn#showMenu">
          <%= image_tag image_path('admin/menu.svg') %>
        </button>
        <%= link_to '/admin' do %>
          <%= image_tag image_path('admin/logo.svg') %>
        <% end %>
        <div style="height: 46px;"></div>
      </div>
      <div class="flex items-center">
        <div class="flex items-center" style="margin-right: 5px;">
          <div style="margin-right: 5px;">
            Hello<span style="font-weight: bold;"> <%= current_user.email %></span>!
          </div>
          <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: 'btn btn-primary' %>
        </div>
        <div data-action="click->dark#change" class="text-gray-500 p-2.5">
          <svg id="theme-toggle-dark-icon" class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
            <path d="M17.293 13.293A8 8 0 016.707 2.707a8.001 8.001 0 1010.586 10.586z"></path>
          </svg>
        </div>
      </div>
    </div>
  </div>
</nav>
