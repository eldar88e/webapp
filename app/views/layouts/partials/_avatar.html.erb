<div class="w-<%= local_assigns[:size] || 8 %> h-<%= local_assigns[:size] || 8 %> rounded-full flex justify-center items-center overflow-hidden flex-shrink-0">
  <% if user.photo_url.present? %>
      <%= image_tag user.photo_url %>
  <% else %>
    <div class="w-full h-full bg-<%= %w[red blue green indigo purple].sample %>-800 flex items-center justify-center text-white font-normal">
      <%= user.first_name&.strip&.first %>
    </div>
  <% end %>
</div>