<%= render partial: '/cart_items/cart_item', collection: @cart_items %>

<div><%= button_to "Очистить заказ", @cart, method: :delete, class: 'btn' %></div>
<%= button_to "Оформить заказ", orders_path, class: 'btn'  %>